<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Button.Rounded" parent="Base.TextAppearance.AppCompat.Widget.Button.Borderless.Colored">
        <item name="android:background">@drawable/bg_rounded_button</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">6dp</item>
        <item name="android:paddingBottom">2dp</item>
        <item name="android:minHeight">32dp</item>
        <item name="android:minWidth">32dp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Button.Rounded.Main">
        <item name="android:layout_marginEnd">38dp</item>
        <item name="android:background">@drawable/bg_rounded_button_left</item>
    </style>

    <style name="Button.Rounded.Image">
        <item name="android:background">@drawable/bg_rounded_button_right</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:layout_gravity">end</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
    </style>
</resources>
